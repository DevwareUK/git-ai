# PRS Rename Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename the repository’s public product surface from `git-ai` to `prs` / Pull Request Smith while preserving compatibility with legacy state paths and managed markers.

**Architecture:** Introduce shared rename constants and compatibility helpers, then update CLI, setup, workflow, and core marker/state callers to use canonical `prs` names for new writes and legacy `git-ai` handling for reads and updates. Finish by refreshing tests, docs, checked-in workflow assets, and built artifacts.

**Tech Stack:** TypeScript, pnpm workspaces, Vitest, tsup, GitHub Actions

---

### Task 1: Shared rename contract

**Files:**
- Modify: `packages/cli/src/index.ts`
- Modify: `packages/cli/src/config.ts`
- Modify: `packages/cli/src/run-artifacts.ts`
- Modify: `packages/core/src/pr-assistant-body.ts`

- [ ] Write failing tests for canonical `prs` naming and legacy compatibility.
- [ ] Run the targeted tests and confirm they fail for the expected old-brand behavior.
- [ ] Add shared constants/helpers for canonical names, legacy names, and marker/path lookup.
- [ ] Re-run the targeted tests and make them pass before moving on.

### Task 2: CLI and package rename

**Files:**
- Modify: `package.json`
- Modify: `packages/*/package.json`
- Modify: `packages/**/*.ts`
- Modify: `actions/*/src/*.ts`

- [ ] Write or update tests proving `prs` is the primary command and `git-ai` still works with a deprecation notice.
- [ ] Rename package scope/imports and primary bin wiring to `prs`.
- [ ] Keep the `git-ai` alias mapped to the same entrypoint with a deprecation warning.
- [ ] Re-run the relevant CLI and package tests.

### Task 3: Setup and managed asset migration

**Files:**
- Modify: `packages/cli/src/setup.ts`
- Modify: `packages/cli/src/setup.test.ts`
- Modify: `.github/workflows/*.yml`

- [ ] Write or update tests for `.prs/` gitignore handling, `prs-*.yml` generation, and migration from legacy managed workflow filenames.
- [ ] Implement canonical setup writes plus managed migration from `git-ai-*.yml` to `prs-*.yml`.
- [ ] Update action refs and workflow headers to `DevwareUK/prs` and `# Generated by prs setup`.
- [ ] Re-run setup tests until they pass.

### Task 4: Docs, checked-in assets, and dist refresh

**Files:**
- Modify: `README.md`
- Modify: `docs/launch-demo.md`
- Modify: `actions/*/README.md`
- Modify: tracked `dist` outputs

- [ ] Update documentation from verified source behavior, including the `git-ai` deprecation path and `.prs/` compatibility behavior.
- [ ] Refresh checked-in workflow fixtures and tracked dist outputs after source changes.
- [ ] Search for stale primary-surface `git-ai` references and resolve any remaining mismatches.

### Task 5: Verification

**Files:**
- Modify: any failing test files or snapshots surfaced during verification

- [ ] Run focused test suites for CLI, setup, state path handling, and marker compatibility.
- [ ] Run `pnpm build`.
- [ ] Review the final diff for accidental scope creep and confirm `.git-ai/` run artifacts were not committed.
